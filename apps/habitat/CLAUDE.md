@AGENTS.md

1. Ask me questions interactively in batches to design features.
2. Use Red/Green TDD to develop features.
3. Be mindful of parallel agents and use git worktrees where possible.
4. Quote commandline parameters appropriately, especially those containing chars such as `[`, `{`, `*`.
5. Run `pnpm test:unit` after meaningful changes and `pnpm check:fix` before finishing work.
